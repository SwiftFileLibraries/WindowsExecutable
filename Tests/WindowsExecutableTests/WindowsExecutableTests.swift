import XCTest
import BinaryCodable
@testable import WindowsExecutable

final class WindowsExecutableTests: XCTestCase {
    
    func testExample() throws {
        // Need to find an executable that tests can be run with.
//        let url = URL(fileURLWithPath: "")
//        let data = try! Data(contentsOf: url)
//        let decoder = BinaryDecoder()
//
//        do {
//            let portableExecutable = try decoder.decode(WindowsPortableExecutable.self, from: data)
//            let resourceSection = portableExecutable.sections.first(where: { $0.header.name.contains("rsrc") })!
//        } catch {
//            XCTFail(error.localizedDescription)
//        }
    }
    
}
